get:
  tags:
    - book
  summary: 全てのbookを取得
  description: ''
  operationId: getAllBooks
  produces:
    - application/json
  responses:
    '200':
      description: 成功
      schema:
        type: array
        items:
          $ref: '#/definitions/Book'
