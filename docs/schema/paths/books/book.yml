get:
  tags:
    - book
  summary: 指定したBookを取得
  description: ''
  operationId: getBookById
  produces:
    - application/json
  parameters:
    - name: bookId
      in: path
      required: true
      type: integer
  responses:
    '200':
      description: 成功
      schema:
        $ref: '#/definitions/Book'